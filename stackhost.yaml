version: 1.0

app:
  name: Bunny_Bot
  language: python  # Required: environment language
  runtime: python-3.10  # Specify the version of Python you're using
  build:
    command: pip install -r requirements.txt
  start:
    command: python bot.py
  env:
    - PYTHONUNBUFFERED=1
