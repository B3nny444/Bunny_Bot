version: 1.0

app:
  name: Bunny_Bot
  build:
    command: pip install -r requirements.txt
  start:
    command: python bot.py
  env:
    - PYTHONUNBUFFERED=1

# Add an explicit 'environment' section
environment:
  language: python
  runtime: python-3.10  # Optional but recommended for clarity